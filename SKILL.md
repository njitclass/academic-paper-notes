---
name: academic-paper-notes
description: "顶刊学术论文深度学习笔记生成器，专为能源工程经济学/管理科学/营销学/MPaCC方向设计。支持工程经济学路线（Nature Energy、Applied Energy：LCOE/系统优化/LCA/博弈/鲁棒优化）与计量实证路线（Energy Economics、TAR、AER等：DID/IV/RDD/面板数据）双模式。当用户说'帮我读这篇文章'、'分析这篇论文'、'生成读书笔记'、'方法论迁移'、'研究切入口'，或上传PDF/粘贴论文内容时使用。"
license: MIT
compatibility: Requires pdf skill for PDF input processing. Compatible with all
  Agent Skills-compliant platforms (Codex CLI, Gemini CLI, Claude Code, Cursor,
  Antigravity, and others).
metadata:
  author: Zhentao Zhu
  version: "1.3.0"
---

# 顶刊学术论文学习笔记

## 工作原则：批判性视角贯穿始终

生成笔记的每个模块时，同步追问以下四个问题，发现的问题积累到模块6集中呈现：

- **假设是否过强？** 识别策略前提是否成立（平行趋势、IV外生性、系统边界合理性）
- **论证是否跳跃？** 从数据/模型到结论之间是否有未经检验的逻辑环节
- **结果是否被设计？** 参数选取、样本范围、边界划定是否系统性有利于结论
- **外部效度如何？** 结论能否推广到其他情境、国家、行业、时间段

顶刊文章通过同行评审并不意味着无懈可击——发现实质性缺陷本身就是高价值的研究切入口。

---

## 步骤0：接收输入

### PDF输入
用户上传PDF时，调用内置 **pdf skill** 提取全文，不自行编写提取脚本。

### 文字输入
用户粘贴摘要或全文时，直接使用，并在笔记开头标注"输入完整度：摘要/部分/全文"。仅有摘要时，在信息不足的字段标注`[摘要未提供，推测]`。

### 批量输入判断
- 用户提供**多篇**内容时 → 进入**批量模式**，执行步骤4的循环逻辑
- 用户提供**单篇**内容时 → 进入**单篇精读模式**

---

## 步骤1：判断方法论路线

根据期刊名和论文关键词，判断走哪条路线：

| 路线 | 触发期刊/特征 |
|------|--------------|
| **模式A**（计量实证） | Energy Economics, Energy Policy, TAR, JAE, CAR, JAR, AER, QJE, RES, JPE, Marketing Science, JMR, AMJ, AMR；关键词：regression, DID, instrumental variable, fixed effects, panel data |
| **模式B**（工程经济学） | Nature Energy, Applied Energy, Energy Conversion and Management, Renewable Energy, Joule；关键词：optimization, LCOE, LCA, energy system, robust optimization, game theory, scenario analysis |
| **模式A+B**（混合型） | Applied Energy等期刊中同时含计量回归和系统优化/LCA的文章 |

判断模糊时：加载 `references/journal-profiles.md` 查询，仍不确定则在笔记开头声明推断结果并请用户确认。

**确定路线后，再执行步骤2。**

---

## 步骤2：生成7模块笔记

> **按需加载规则（在生成对应模块前执行）：**
> - 模式含A → 生成模块3/4前，读取 `references/method-econometrics.md`
> - 模式含B → 生成模块3/4前，读取 `references/method-engineering-econ.md`
> - 生成模块5前 → 读取 `references/transfer-guide.md`
> - 上述加载后即可生成，不需要预先全部加载

---

### 模块1：📌 题录信息

```
作者, 年份. 文章标题. 期刊名称, 卷(期), 页码. DOI
期刊档位：[Nature Index顶刊 / JCR Q1 / Q2]  影响因子：[IF]
论文类型：[实证研究 / 建模优化 / 混合方法 / 综述]
输入完整度：[全文 / 部分 / 摘要]
数据可得性：[公开数据集: URL / 需申请 / 未公开]
代码可得性：[GitHub: URL / 补充材料 / 未提供]
```

---

### 模块2：🎯 研究问题与理论贡献

```
核心问题：[一句话，以"本文研究..."起头]
研究背景：[现实问题 + 文献空白，2-3句]
理论机制：[为什么X导致Y，或模型的核心逻辑]
核心假设：[列出1-3个关键假设，后续批判性追问的起点]
文献贡献：
  □ 相比[具体文献]，本文新增了[具体贡献]
  □ 方法创新：[如适用]
  □ 数据/情境创新：[如适用]
理论框架/核心公式：[LaTeX，如适用]
```

---

### 模块3：📐 数据与研究方法

**按路线展开——** 模式A见 `references/method-econometrics.md` 模板，模式B见 `references/method-engineering-econ.md` 模板，模式A+B两套并列输出，各自标注`[计量部分]`和`[工程建模部分]`。

此模块是全文最重要的模块，要求：
- 所有公式用LaTeX写出，不用文字代替
- **每个公式之后必须紧跟符号说明**，逐行列出每个符号的含义、单位（如适用）及典型取值范围（如适用），不得出现无说明的裸公式
- 识别策略/约束方程须完整，不可省略关键条件
- 参数来源须注明（实测/文献/假设），为批判性评估积累素材

---

### 模块4：📊 主要结果与检验

**按路线展开——** 同模块3，分别按 `references/method-econometrics.md` 和 `references/method-engineering-econ.md` 中的结果模板呈现。

核心要求：
- 结论须量化（系数值/LCOE数值/优化目标值），不接受纯文字描述
- 检验/敏感性分析须列出清单，注明通过/未通过

---

### 模块5：🔬 方法论迁移地图

> 生成本模块前，读取 `references/transfer-guide.md`

```
本文可迁移的方法论亮点（1-3个）：
  1. [方法名] — 可迁移原因：[为什么这个设计在其他场景也适用]

迁移建议：
  → 能源工程经济学方向：
     课题：[具体题目级别的建议]
     所需数据：[具体说明]
     期刊目标：[具体期刊]

  → 能源政策/计量方向：
     课题：[具体题目级别的建议]

  → 管理科学/营销方向：
     课题：[具体题目级别的建议]

  → MPaCC/会计方向：
     课题：[具体题目级别的建议]
     入门难度：[适合MPaCC学生 / 需要较强方法基础]

迁移前提条件：[数据可得性要求、场景匹配限制]
不适合迁移的情境：[明确说明边界]
```

---

### 模块6：🔍 批判性评估与研究切入口

这是将阅读转化为研究生产力的核心模块，综合前5个模块分析过程中积累的批判性观察。

```
方法论局限性：
  □ [假设X过强：具体指出该假设、为何存疑、对结论的影响方向]
  □ [识别策略/模型设定缺陷：具体描述]
  □ [外部效度限制：结论适用范围]

理论局限性：
  □ [机制论证跳跃/黑箱：具体位置]

顶刊亮点（反向学习）：
  □ [这篇文章在[X]方面的处理特别出色，值得在自己的写作中借鉴]

缺陷 → 研究切入口：
  每个实质性缺陷必须给出切入方向，格式：
  【缺陷】[具体描述]
  【切入】[可改进的具体研究问题]
  【期刊】[适合投稿的目标期刊]
```

如果本文没有发现实质性缺陷，需说明理由，并标注"本文方法论较为严谨，切入口方向以延伸拓展为主（见模块7）"。

---

### 模块7：💡 研究灵感笔记

```
直接启发的研究问题（3-5个，须可操作）：
  1. [问题] — 数据来源：[具体] — 难度评估：[教授课题/学生课题]
  2. ...

可延伸的情境/数据：
  □ 中国情境适配：[如原文为欧美数据，中国类似数据集是什么]
  □ 行业/技术延伸：[从X技术扩展到Y技术]
  □ 时间维度延伸：[更长时间序列/实时数据]

与当前研究热点的交叉：
  □ [热点方向] × 本文方法 → [潜在选题]

MPaCC学生入门建议：
  推荐切入点：[选最简单可操作的一个方向]
  所需前置知识：[具体列出]
  预计工作量：[X个月]
```

---

## 步骤3：写入文件

### 文件命名
```
note-{第一作者姓}-{发表年份}-{标题前5实词，空格→_}.md
```
实词定义：跳过冠词(a/an/the)、介词(of/in/for/on等)、连词。
示例：`note-Zhang-2024-Carbon_trading_scheme_optimal_design.md`

### 文件写入要求（所有平台强制执行）

生成笔记内容后，**必须使用平台原生文件写入工具将完整7模块内容写入磁盘**，
不得仅向对话流输出。

各平台写入方式：
- **Claude Code**：使用 `Write` 工具写入文件
- **Codex CLI / Gemini CLI**：使用 Python `open().write()` 工具调用，
  或支持文件操作的原生工具
- **其他平台**：使用该平台提供的文件写入 API / 工具调用

写入后必须：
1. 输出文件**绝对路径**供用户验证
2. 确认文件存在（如平台支持，调用文件读取工具验证）

> ⚠️ **严禁将完整7模块笔记内容输出到对话流。禁止使用 bash heredoc 模板
> （单引号定界符禁止变量展开，无法注入 Agent 生成的内容）。**

---

### 单篇精读模式 / 批量模式（输出格式统一）

> ⚠️ **强制要求，适用于单篇与批量所有模式：**
> 1. 完整7模块笔记内容**必须写入 `.md` 文件**，不得输出到对话流
> 2. 对话流中**仅允许**出现：文件绝对路径 + 下方摘要格式块
> 3. 单篇模式与批量模式执行相同的写文件逻辑，不因"只有一篇"而豁免
> 4. 写入完成后**必须输出绝对路径**，供用户验证文件是否存在

两种模式均采用相同输出策略：
- 笔记**写入 `.md` 文件**（完整7模块内容，使用上方"文件写入要求"中的方式执行）
- 对话中**只允许**输出：**文件绝对路径 + 约400字结构化摘要**，格式：

```
✅ [X/N] note-Zhang-2024-... 已生成（单篇时省略进度标记）
📁 文件路径：[绝对路径，如 /Users/xxx/papers/note-Zhang-2024-xxx.md]
└─ 核心问题：...
└─ 方法：[模式A/B/AB] | [识别策略/核心模型，1句]
└─ 主要结论：[量化结论，2-3句]
└─ 最强切入口：[1个最具价值的研究切入，含目标期刊]
└─ 迁移亮点：[1个最可操作的迁移建议]
└─ 顶刊亮点：[1个值得借鉴的写作/方法呈现亮点]
```

摘要设计保证后续追问无需重新读取文件；如需查看详情，直接打开文件链接。

---

## 步骤4：批量循环逻辑

### 对话模式（Cowork / 不支持文件系统访问的平台）

用户提交N篇时，**立即计算并告知**：
```
📋 检测到N篇论文，需N轮处理（每轮1篇）。
   本轮：第1篇 → 完成后请继续提交第2篇。
```

每轮处理1篇，完成后提示下一轮所需操作。循环由用户手动触发。

### Agent 自动执行模式（支持文件系统与工具调用的平台）

用户指定项目文件夹时，执行以下逻辑：

```bash
# 1. 扫描所有PDF，计算总数
PDF_FILES=($(ls *.pdf 2>/dev/null))
TOTAL=${#PDF_FILES[@]}

if [ $TOTAL -eq 0 ]; then
    echo "❌ 当前目录未发现PDF文件，请 cd 到正确目录后重试"
    exit 1
fi

echo "📋 共发现${TOTAL}篇PDF，开始逐篇处理"
echo "📁 输出目录：$(pwd)"

# 2. 逐篇循环（每次1篇）
for ((i=0; i<TOTAL; i++)); do
    PDF="${PDF_FILES[$i]}"
    echo "--- 第$((i+1))/${TOTAL}轮：${PDF} ---"

    # [调用pdf skill提取全文 → 执行步骤1-3]
    # [FILENAME 由步骤3命名规则生成：note-{作者姓}-{年份}-{标题前5实词}.md]
    # [使用步骤3"文件写入要求"中的平台原生写入工具写入文件]

    echo "✅ [$((i+1))/${TOTAL}] ${FILENAME} 已写入 $(pwd)"
done

echo "🎉 全部完成：共${TOTAL}篇，生成${TOTAL}个.md文件"
echo "📁 文件位置：$(pwd)"
```

全部完成后，在对话末尾追加**跨文比较摘要**（方法论异同 + 可组合的研究方向），不单独生成文件。

---

## 追问交互规范

笔记末尾固定追加以下提示（批量模式中在最后一批完成后追加一次）：

```
---
💬 可继续追问：
  · "详细展开模块[N]的[具体内容]"
  · "给我解释[方法名]的直觉原理"（学生友好版）
  · "这个[识别策略/优化模型]的具体实现步骤"
  · "帮我写一段引用这篇文章的lit review段落"
  · "把切入口[X]展开成一个完整的研究设计草案"
  · "比较这篇和[另一篇]的方法论差异"
```

---

## 按需加载总览

| 文件 | 加载时机 | 不加载时机 |
|------|----------|-----------|
| `references/method-econometrics.md` | 模式含A，生成模块3/4前 | 纯模式B文章 |
| `references/method-engineering-econ.md` | 模式含B，生成模块3/4前 | 纯模式A文章 |
| `references/transfer-guide.md` | 生成模块5前（每篇都加载） | — |
| `references/journal-profiles.md` | 步骤1期刊判断模糊时 | 期刊可直接判断时 |
| **pdf skill**（内置） | 用户上传PDF时 | 文字输入时 |
