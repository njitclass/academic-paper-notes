# 计量/实证经济学方法解析模板（路线A）

适用期刊：Energy Economics, Energy Policy, TAR, JAE, CAR, JAR, AER, QJE, RES, JPE, Marketing Science, JMR, AMJ, AMR

---

## 模块3A：数据与实证策略

### 3A-1 数据描述

```
数据来源：[数据库名称/来源机构]
样本范围：[国家/地区/行业/企业] × [时间范围]
观测单位：[个体/企业/省份/国家]
样本量：N=[个体数], T=[时间期数], 总观测=[N×T]
关键变量描述统计：[均值/标准差/数据来源，若原文有表格则摘录]
```

### 3A-2 识别策略（内生性问题与解决方案）

首先明确内生性来源：□ 遗漏变量  □ 反向因果  □ 测量误差  □ 样本选择

然后按使用的识别工具填写：

**DID（双重差分）**
```
处理组/控制组定义：[具体]
政策冲击/事件：[时间节点]
基准回归：
  $$Y_{it} = \alpha + \beta \cdot Treat_i \times Post_t + \gamma X_{it} + \mu_i + \lambda_t + \varepsilon_{it}$$
  符号说明：
  - $Y_{it}$：被解释变量，含义：[填入]，单位：[填入]
  - $Treat_i$：处理组虚拟变量（处理组=1，控制组=0）；定义：[填入]
  - $Post_t$：政策后虚拟变量（政策实施后=1）；时间节点：[填入]
  - $\beta$：DID核心估计量，识别ATT（平均处理效应），含义：[具体解释]
  - $X_{it}$：控制变量向量，包含：[逐一列出]
  - $\mu_i$：个体固定效应；$\lambda_t$：时间固定效应；$\varepsilon_{it}$：误差项
  核心系数 $\beta$ 的含义：[ATT解释]
平行趋势检验：□ 已做事件研究图  □ 未做 ← 批判性注意
预处理期系数是否显著不为零：[是/否/未报告]
异质性DID（Callaway-Sant'Anna等）：□ 使用  □ 未使用 ← 多期处理必查
```

**工具变量（IV）**
```
工具变量：[具体变量名]
相关性论证：[为什么Z与X相关，是否有F统计量，F > 10？]  ← 弱工具变量检验
外生性论证：[排除性约束是否可信？有无直接检验？]  ← 批判性核心
第一阶段结果：F=[值]
Sargan/Hansen检验（过识别时）：[p值]
```

**PSM（倾向得分匹配）**
```
匹配变量：[列出]
匹配方法：□ 最近邻  □ 核匹配  □ 卡尺匹配
匹配后平衡性检验：□ 标准化偏差 < 10%  □ 未报告
共同支撑假设：□ 检验  □ 未检验  ← 批判性注意
```

**RDD（断点回归）**
```
运行变量：[具体变量名]
断点值：[阈值]
带宽选择：[方法，如IK/CCT最优带宽]
局部线性回归 vs 多项式：[阶数]
操纵检验（McCrary test）：□ 通过  □ 未做  ← 必查
```

**事件研究**
```
事件窗口：[-T, +T]
正常收益估计窗口：[时间段]
超额收益计算方法：[市场模型/Fama-French等]
```

### 3A-3 基准回归模型

```
基准方程（LaTeX完整写出）：
  $$Y_{it} = \alpha + \beta_1 X_{it} + \sum_k \gamma_k Control_{kit} + \mu_i + \lambda_t + \varepsilon_{it}$$
  符号说明：
  - $Y_{it}$：被解释变量，含义：[填入]，单位/量纲：[填入]
  - $X_{it}$：核心解释变量，含义：[填入]，测量方式：[填入]
  - $\beta_1$：核心参数，经济含义：[X增加1单位时Y的变化量/比例]
  - $Control_{kit}$：第 $k$ 个控制变量，共[K]个，分别为：[逐一列出]
  - $\gamma_k$：控制变量对应系数
  - $\mu_i$：个体固定效应，控制不随时间变化的个体异质性
  - $\lambda_t$：时间固定效应，控制共同时间趋势
  - $\varepsilon_{it}$：误差项，聚类方式：[个体/双维/空间]

固定效应设置：
  □ 个体固定效应 $\mu_i$
  □ 时间固定效应 $\lambda_t$
  □ 行业×年份交互固定效应
  □ 其他：[  ]

标准误处理：
  □ 个体聚类  □ 双维聚类（个体+时间）  □ 空间相关  □ 异方差稳健
  选择是否合适：[判断] ← 批判性注意
```

---

## 模块4A：主要结果与稳健性检验

```
核心回归结果：
  主要系数 $\hat{\beta}_1$ = [值]，标准误 = [值]，[***/**/*]
  符号说明：$\hat{\beta}_1$ 为OLS/2SLS/FE估计量，含义：[$X_{it}$ 增加1[单位]，$Y_{it}$ 变化[值][单位]]
  经济显著性：[效应量解释，如"X增加1个标准差，Y变化Z%"]

异质性分析：
  分组变量：[按什么分组]
  结论：[不同子组的系数差异及解释]

稳健性检验清单：
  □ 替换核心变量测量方式：[结论是否稳定]
  □ 安慰剂检验：[随机处理/伪政策/时间置换，p值分布是否集中在0附近]
  □ 子样本稳健性：[排除[X]后结论是否稳定]
  □ 不同固定效应规格：[结论是否稳定]
  □ 不同样本期：[结论是否稳定]
  ← 哪些稳健性检验缺失？这是批判性评估的重要来源

机制检验：
  中介变量：[M]，$\beta_{X \to M}$ = [值]，$\beta_{M \to Y}$ = [值]
  调节变量：[W]，交互项系数 = [值]
  机制论证是否充分：[是否只有相关性而无法排除混淆机制] ← 批判性注意
```
