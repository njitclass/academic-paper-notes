# 工程经济学/能源系统方法解析模板（路线B）

适用期刊：Nature Energy, Applied Energy, Energy Conversion and Management, Renewable Energy, Joule

---

## 模块3B：研究方法与模型架构

### 3B-1 研究系统界定

```
系统边界：[地理范围 / 行政单元 / 技术系统边界]
时间跨度：[规划年限 / 仿真步长 / 数据年份]
技术路线：[所研究的能源技术组合]
功能单元（LCA适用）：[如"每产生1MWh电力"]
```

### 3B-2 核心方法论识别

从以下类型中识别，可多选：

**A. 技术经济评价**
```
评价指标：□ LCOE  □ LCOS  □ NPV  □ IRR  □ 平准化成本  □ 其他:[  ]
成本结构：
  CAPEX = [初始投资组成，LaTeX]
  OPEX  = [年运营成本组成，LaTeX]
  LCOE核心公式：
    $$\text{LCOE} = \frac{\sum_{t=0}^{T} \frac{C_t}{(1+r)^t}}{\sum_{t=0}^{T} \frac{E_t}{(1+r)^t}}$$
    符号说明：
    - $\text{LCOE}$：平准化度电成本（$/MWh 或 $/kWh）
    - $C_t$：第 $t$ 年总成本（$），含CAPEX摊销与OPEX，具体构成：[填入]
    - $E_t$：第 $t$ 年发电量（MWh），由[装机容量×容量因子]决定
    - $r$：折现率（%），本文取值：[填入]，来源：[假设/文献]
    - $T$：项目评估期（年），本文取值：[填入]
    - 若为LCOH/LCOS等变体，相应替换分子/分母并说明
关键参数取值：[逐一列出，注明来源：实测/文献/假设]
```

**B. 系统优化建模**
```
优化类型：□ LP  □ MILP  □ 多目标优化  □ 随机优化  □ 鲁棒优化  □ 分布鲁棒优化
目标函数：
  $$\min / \max \quad f(x) = [具体表达式，LaTeX]$$
  符号说明：
  - $x$：决策变量向量，含：[逐一列出各分量及其物理含义与单位]
  - $f(x)$：优化目标，含义：[成本/效益/碳排放等]，单位：[填入]
  - [其余目标函数中出现的符号逐行说明]
决策变量：[列出核心变量及其物理含义]
物质与能量平衡约束（须完整列出，每个约束后跟符号说明）：
  能量守恒：$$\sum P_{in} \cdot \eta = \sum P_{out} + P_{loss}$$
    - $P_{in}$：输入功率（kW）；$\eta$：转换效率（%）；$P_{loss}$：损耗（kW）
  功率平衡：$$P_{gen,t} + P_{dis,t} = P_{load,t} + P_{ch,t}$$
    - $P_{gen,t}$：$t$时段发电功率（kW）；$P_{dis,t}$：储能放电（kW）
    - $P_{load,t}$：负荷需求（kW）；$P_{ch,t}$：储能充电（kW）[如适用]
  物质守恒：[碳/氢/氧元素平衡，LaTeX] + [符号说明，如适用]
容量约束：
  $$0 \leq P_t \leq P^{cap} \cdot u_t, \quad u_t \in \{0,1\}$$
    - $P^{cap}$：额定装机容量（kW）；$u_t$：0-1启停变量；$P_t$：实际出力（kW）
爬坡/动态约束：[LaTeX公式] + [符号说明，如适用]
储能充放电约束：[LaTeX公式，含SOC约束] + [符号说明，如适用]
网络/潮流约束：[如适用]
求解方法：[CPLEX/Gurobi/自编Python+Pyomo/MATLAB等] → 注意：原文是否用商业软件还是自编程
```

**C. 生命周期分析（LCA）**
```
LCA标准：□ ISO 14040/44  □ 其他
系统边界类型：□ 摇篮到大门  □ 摇篮到坟墓  □ 摇篮到摇篮
功能单元：[定义]
影响类别：[列出所评估的环境影响指标]
数据库：[ecoinvent / GaBi / 自建 / 其他]
关键排放因子：[列出核心数值及来源]
```

**D. 博弈论模型**
```
博弈类型：□ Nash均衡  □ Stackelberg博弈  □ 合作博弈  □ 机制设计  □ 重复博弈
参与方：[列出博弈主体及其策略空间]
领导者/跟随者（Stackelberg适用）：[角色定义]
效用/利润函数：
  $$\pi_i = [具体表达式，LaTeX]$$
  符号说明：
  - $\pi_i$：参与方 $i$ 的效用/利润，单位：[元/$/效用单位]
  - [逐一说明公式中各符号：价格变量、产量变量、成本参数等，含单位和取值范围]
均衡条件：[KKT条件 / 反应函数 / 纳什均衡定义，LaTeX] + [关键符号说明]
求解方法：[解析解 / 数值迭代 / 逆向归纳]
```

**E. 鲁棒优化与不确定性分析**
```
不确定性来源：[列出不确定参数，如可再生能源出力、需求、价格]
建模方式：
  □ 鲁棒优化（RO）：不确定集定义 $\mathcal{U} = \{...\}$
    - $\mathcal{U}$：不确定集，含义：[盒式/椭球/多面体]；所含不确定参数：[列出]
    - $\Gamma$：保守性参数，取值：[填入]，含义：[最多有多少参数同时偏离名义值]
  □ 机会约束规划：$\Pr[g(x,\xi) \leq 0] \geq 1-\epsilon$
    - $\xi$：随机参数向量；$g(x,\xi)$：约束函数；$\epsilon$：违约概率，取值：[填入]
  □ 分布鲁棒优化（DRO）：矩集/Wasserstein球
    - 模糊集定义：[矩约束形式 / Wasserstein半径 $\rho$，含义与取值]
  □ 区间分析：参数范围 $[\underline{a}, \bar{a}]$
    - $\underline{a}$：参数下界；$\bar{a}$：参数上界；区间来源：[假设/历史数据/专家估计]
  □ 信息间隙决策理论（IGDT）：$\hat{\alpha}$ 的含义
    - $\hat{\alpha}$：最大可容忍不确定性水平；$r_c$：临界绩效目标，取值：[填入]
保守性参数：[如鲁棒优化的 $\Gamma$ 值，含义及取值]
```

**F. 数据驱动/机器学习**
```
预测目标：[负荷/出力/价格等]
模型类型：[LSTM/XGBoost/物理-数据混合模型等]
训练/测试集划分：[比例 + 时间范围]
评价指标：[RMSE/MAE/R²等]
```

### 3B-3 参数与数据来源汇总

列出所有关键参数，注明来源类别：
- **实测数据**：[来自哪里]
- **技术参数库**：[NREL/IRENA/IPCC/文献引用]
- **统计数据库**：[IEA/EIA/国家统计局等]
- **作者假设**：[哪些参数是假设值，是否做了敏感性测试] ← 批判性追问重点

---

## 模块4B：主要结论与敏感性分析

```
核心量化结论（须给出具体数值）：
  [结论1]：[数值 + 单位 + 与基准/文献对比的%变化]
  [结论2]：...

关键驱动因素排序：
  [按敏感性分析结果，列出对目标函数影响最大的3-5个参数]

情景/方案对比（如适用）：
  | 情景 | 关键指标1 | 关键指标2 | 说明 |
  |------|----------|----------|------|
  | 基准情景 | ... | ... | |
  | 情景A | ... | ... | |

敏感性分析结果：
  □ 单因素敏感性：[最敏感参数及敏感性系数]
  □ 蒙特卡洛：[概率分布类型 + 95%置信区间]
  □ 情景边界：[最优/最差情景下结论是否仍成立]
  □ 鲁棒性（RO/IGDT）：[在何种不确定性水平下结论仍有效]

与文献的对比：
  [本文LCOE/结论 vs. 近期同类文章，定量说明差异原因]

政策含义：[1-2条具体的政策建议，须与量化结论直接挂钩]
```
